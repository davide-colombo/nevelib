# Claude Code adapter

Read and follow the root and applicable nested `AGENTS.md` files, then
`.agents/PROJECT_PROFILE.md`. Read `.agents-local/PROJECT_PROFILE.local.md` only
when it exists. `.claude/skills` is generated from the pinned canonical source and
must not be edited manually.
